import { Provider } from "react-aria-components";

/**
 * The StyleProvider component is a utility that makes it easier to provide multiple React contexts without manually nesting them. This can be achieved by passing pairs of contexts and values as an array to the values prop.
 *
 * [View Documentation](TODO)
 */
export const StyleProvider = Provider;
