import { Meta } from '@storybook/blocks';
import { DesignTokenDocBlock } from 'storybook-design-token';

<Meta title="Tokens/Colors/Light" />

# Colors - Light

Workleap light color tokens are used to maintain a consistent color palette across the application.

<DesignTokenDocBlock categoryName="Colors" viewType="card" pageSize={Number.MAX_VALUE} />
