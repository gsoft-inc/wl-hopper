---
title: Playground
description: Content made easy for developers
---

### With Title, Caption and line numbers

```ts showLineNumbers {3} /levels/ title="index.ts" caption="Don't reassign const."
const levels = 3;
const a = 1;
const a = 2;
const a = 4;
```

### Start line numbers at a specific line number

```js showLineNumbers{42}"
const levels = 3;
const weapons = 10;
```

### Inline Code Highlighting

The result of `[1, 2, 3].join('-'){:js}` is `'1-2-3'{:js}`.

### Group highlighted words by id

```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /'Taylor'/#i
const [age, setAge] = useState(50);
const [name, setName] = useState('Taylor');
```

### Highlight plain text

The name of this function is `getStringLength{:.entity.name.function}`.

### Context aware inline-code

```js
function getStringLength(str) {
  return str.length;
}
```

When we refer to `getStringLength{:.entity.name.function}` as a plain variable,
we can color it as a function.

### JSON

```json title="package.json"
{
    "name": "wl-hopper",
    "version": "0.0.0",
    "description": "",
    "keywords": [],
    "author": "",
    "private": true,
    "license": "Apache-2.0",
    "packageManager": "pnpm@8.3.1",
    "scripts": {
        "doc:start": "pnpm --filter=docs dev",
        "doc:storybook": "pnpm --filter=docs storybook",
        "doc:build": "pnpm --filter=docs build",
        "lint": "pnpm run \"/^lint:.*/\"",
        "lint:eslint": "eslint . --ext .js,.jsx,.ts,.tsx  --cache --cache-location node_modules/.cache/eslint",
        "lint:types": "pnpm -r lint:types",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@netlify/plugin-nextjs": "^4.36.0",
        "@workleap/eslint-plugin": "^1.0.0",
        "@workleap/typescript-configs": "^1.0.0"
    }
}
```

### CSS

```css  {1-3} /var(--hd-space-1)/
:root {
    --hd-space-1: 0.25rem;
}

[data-rehype-pretty-code-figure] pre {
    border-radius: var(--hd-space-1);
}
```

### All Extensions

```c
#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   printf("Hello, World!");
   return 0;
}
```

```csharp
using System;

namespace HelloWorld
{
  class Program
  {
    static void Main(string[] args)
    {
      Console.WriteLine("Hello World!");
    }
  }
}
```

```css
:root {
    --hd-space-1: 0.25rem;
}

[data-rehype-pretty-code-figure] pre {
    border-radius: var(--hd-space-1);
}
```

```diff
> This is an important
> notice! It should
> therefore be located at
> the beginning of this
> document!
>
11,15d16
< This paragraph contains
< text that is outdated.
< It will be deleted in the
< near future.
<
17c18
< check this dokument. On
---
> check this document. On
24a26,29
>
> This paragraph contains
> important new additions
> to this document.
```

```dockerfile
RUN /bin/bash -c 'source $HOME/.bashrc && \
echo $HOME'
```

```unknown
Hello world!
```

```html
<button>Click me!</button>
```

```js
const levels = 3;
const a = 1;
```

```jsx
const levels = 3;
const a = 1;
```

```markdown
### Markdown
```

```php
<?php
  echo "Hello!<br>";
?>
```

```powershell
Write-Host “Hello World!”
```

```python
x = 1
y = 2.8
z = 1j

print(type(x))
print(type(y))
print(type(z))
```

```ruby
def sum_eq_n?(arr, n)
  return true if arr.empty? && n == 0
  arr.product(arr).reject { |a,b| a == b }.any? { |a,b| a + b == n }
end
```

```scss
.sass {
  .test {
    background: red;
  }
}
```

```shell
$ chmod a+x First.sh
$ ./First.sh
```

```sql
SELECT CustomerName, City FROM Customers;
```

```typescript
interface User {
  name: string;
  id: number;
}

const user: User = {
  name: "Hayes",
  id: 0,
};
```

```ts
interface User {
  name: string;
  id: number;
}

const user: User = {
  name: "Hayes",
  id: 0,
};
```

```tsx
interface User {
  name: string;
  id: number;
}

const user: User = {
  name: "Hayes",
  id: 0,
};
```

```yaml
docker:
    - image: ubuntu:14.04
    - image: mongo:2.6.8
      command: [mongod, --smallfiles]
    - image: postgres:9.4.1
```
